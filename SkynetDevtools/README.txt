Skynet Server Controller Script(snsc)

Types:
view,
restful

Function_Name(Parameters)@ControllerType
{
    //Codes Here(JavaScript)
    //Default Support:SkynetMySQL,ResponseData(In a restful controller must return a json string,but in a view controller just need to return a resources path)
}

Use 'node SkynetServerScriptCompiler.js -f FilePath' to compile a file or use 'node SkynetServerScriptCompiler.js -d FolderPath' to compile a folder.